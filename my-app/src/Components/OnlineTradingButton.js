import { React } from "react";
import { Button } from "@mui/material";

export default function OnlineTradingButton() {
    return (
        <Button variant="contained" className='trading-button' color='primary'>
            Online trading
        </Button>
    )
}